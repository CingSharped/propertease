from application.models.Transaction import Transaction

def create_transaction(property_id, property_owner_id, month, money, transaction_type, workorder_id):
  # check type options
  # create transaction class with data
  # run create_transaction and return it